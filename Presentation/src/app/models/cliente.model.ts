import { Endereco } from "./endereco.model";

export class Cliente {
    public constructor(
        idCliente?: number,
        nome?: string,
        documento?: string,
        endereco?: Endereco
    ) {}
}